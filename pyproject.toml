[project]
name = "pytube-implementation"
version = "0.1.0"
description = "YouTube video/audio/playlist downloader using pytubefix"
requires-python = ">=3.10"

dependencies = [
    "pytubefix>=8.0.0",
]

[tool.uv]
package = false
